# This override file should be used when running this service in isolation
# to provide dependencies and expose ports for local testing

services:
  aphw-ddi-notification:
    build:
      target: development
    image: aphw-ddi-notification-development
    container_name: aphw-ddi-notification-development
    ports:
      - "9229:9229"
    volumes:
      - ./app:/home/node/app
      - ./package.json:/home/node/package.json
